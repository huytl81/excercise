<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- Rule for Property Users: Can only read properties -->
        <record id="rule_property_user" model="ir.rule">
            <field name="name">Property Rules for Users</field>
            <field name="model_id" ref="model_estate_property"/>
            <field name="domain_force">[('seller_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_estate_property_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Rule for Property Managers: Full access to all properties -->
        <record id="rule_property_manager" model="ir.rule">
            <field name="name">Property Rules for Managers</field>
            <field name="model_id" ref="model_estate_property"/>
            <field name="groups" eval="[(4, ref('group_estate_property_manager'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>