odoo.define('library_portal', function (require) {
    let core = require('web.core');
    alert(core._t('Hello world'));
    return {
        // if you created functionality to export, add it here
    }
});